# Auth token is NOT stored here: pnpm refuses to expand env vars in a committed
# project .npmrc. Set it once in your user-level ~/.npmrc instead:
#   pnpm config set "//npm.pkg.github.com/:_authToken" "<your write:packages PAT>"
@appelent:registry=https://npm.pkg.github.com
