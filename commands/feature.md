---
description: Appelent feature catalog — list, show, apply, capture
argument-hint: "help | list | show <feature> | apply <feature> [options] | capture <topic>"
---

Use the `appelent-feature` skill from this plugin to handle: $ARGUMENTS

If no arguments were given, run the `help` subcommand.
