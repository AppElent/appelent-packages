{
	"name": "appelent-packages",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "pnpm -r build",
		"test": "pnpm -r test",
		"typecheck": "pnpm -r typecheck",
		"lint": "biome check .",
		"validate:catalog": "node scripts/validate-catalog.mjs && node --test tests/catalog.test.mjs",
		"publish:cli": "pnpm --filter @appelent/cli build && pnpm --filter @appelent/cli publish --no-git-checks"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.0"
	}
}
